<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title or "Team Hub" }}</title>

    {# ✅ Use relative path so it works locally and on GitHub Pages #}
    <link rel="stylesheet" href="{{ 'styles.css' | url }}" />
  </head>
  <body class="bg-gray-100 text-gray-900 font-sans leading-relaxed">
    <header class="bg-white shadow-md py-4 mb-8">
      <div class="max-w-5xl mx-auto px-4">
        <h1 class="text-xl font-bold text-indigo-700">
          {{ title or "Team Hub" }}
        </h1>
      </div>
    </header>

    <main class="max-w-5xl mx-auto px-4 prose prose-custom">
      {{ content | safe }}
    </main>

    <footer class="mt-12 text-center text-sm text-gray-500 py-8">
      &copy; {{ (collections.all | first).date | date("yyyy") if (collections.all | first).date else "2025" }} Team Hub. All rights reserved.
    </footer>
  </body>
</html>
